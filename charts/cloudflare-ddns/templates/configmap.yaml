apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloudflare-ddns.fullname" . }}-script
  labels:
    {{- include "cloudflare-ddns.labels" . | nindent 4 }}
data:
  cloudflare-ddns.sh: |-
{{ .Files.Get "charts/cloudflare-ddns/cloudflare-ddns.sh" | indent 4 }}
