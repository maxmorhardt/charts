apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-script
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  cloudflare-cidr.sh: |-
{{ .Files.Get "cloudflare-cidr.sh" | indent 4 }}
